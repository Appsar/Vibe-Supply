export interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  image_url: string;
  stock: number;
  sku: string;
  created_at: string;
}

//Model for Product and everything it needs
